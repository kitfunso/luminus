# Luminus v0.3.1

Unified npm and Python patch release for the Northern Powergrid headroom tranche and stale package-surface cleanup.

## What shipped

- published `luminus-mcp@0.3.1`
- published `luminus-py==0.3.1`
- added Northern Powergrid public headroom support to `get_distribution_headroom`
- added matching source metadata and health checks in `verify_gis_sources`
- corrected the npm package description and README surface so the published package page reflects the current 62-tool catalog
- updated the Python package surface to the same release line for consistent install docs and provenance

## Release summary

- npm: `luminus-mcp@0.3.1`
- PyPI: `luminus-py==0.3.1`
- git tag: `v0.3.1`

## Verification

- `npx vitest run src/tools/distribution-headroom.test.ts src/tools/grid-connection-intelligence.test.ts src/tools/verify-gis-sources.test.ts src/index.test.ts src/lib/profiles.test.ts`
- `npm run build`
- `python -m pytest python/tests -q`
- `python -m build python`

## Install

```bash
npm install -g luminus-mcp@0.3.1
pip install "luminus-py[notebook]==0.3.1"
```
